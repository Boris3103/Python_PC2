# Problema 1: Eliminar duplicados de una lista

lista_original = [1, 1, 2, 3, 4, 4, 5, 1]

# Convertimos la lista a un conjunto para eliminar duplicados

lista_sin_duplicados = list(set(lista_original))

print("Lista original:", lista_original)
print("Lista procesada:", lista_sin_duplicados)